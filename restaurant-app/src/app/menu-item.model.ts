export interface MenuItem {
    id: number;        // Adjust the type and name based on your database schema
    name: string;
    item_price: number; // Adjust the type and name based on your database schema
  }
  